Feature('문의글 작성');

Scenario('문의글을 공개글로 작성하는 경우', ({ I }) => {
  // Given

  // When

  // Then

});

Scenario('문의글을 비밀글로 작성하는 경우', ({ I }) => {
  // Given

  // When

  // Then

});

Scenario('문의글의 제목과 내용을 입력하지 않은 경우', ({ I }) => {
  // Given

  // When

  // Then

});
