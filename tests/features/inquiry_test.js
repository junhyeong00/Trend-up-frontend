Feature('문의 글 조회');

Scenario('문의 글이 없는 경우', ({ I }) => {
  // Given

  // When

  // Then

});

Scenario('문의 글이 있는 경우', ({ I }) => {
  // Given

  // When

  // Then

});

Scenario('내 문의글 조회를 하는 경우', ({ I }) => {
  // Given

  // When

  // Then

});
