Feature('상품 주문 - 상품을 구매하려는 사람은 상품을 구매하기 위해 주문 / 결제를 완료할 수 있다.');

beforeEach(({ I }) => {

});

Scenario('상품 주문 완료', ({ I }) => {
  // When

  // Then
});

Scenario('', ({ I }) => {
  // Given

  // When

  // Then

});
