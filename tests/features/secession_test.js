Feature('회원 탈퇴');

Scenario('회원탈퇴 동의를 하지 않은 경우', ({ I }) => {
  // Given

  // When

  // Then

});

Scenario('배송 중인 상품이 있는 경우', ({ I }) => {
  // Given

  // When

  // Then

});

Scenario('회원탈퇴 완료', ({ I }) => {
  // Given

  // When

  // Then

});
