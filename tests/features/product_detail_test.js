Feature('product_detail - 상품을 사려는 사람은 상품을 구매하기 위해 상품의 세부 정보를 확인할 수 있다.');

Scenario('리뷰가 없는 경우', ({ I }) => {
  // Given

  // When

  // Then

});

Scenario('상품에 3점, 2점 리뷰가 1개씩 있는 경우', ({ I }) => {
  // Given

  // When

  // Then

});

Scenario('로그인되지 않은 상태 -> 찜, 장바구니, 문의 사용시 로그인 제안 화면', ({ I }) => {
  // Given

  // When

  // Then

});
