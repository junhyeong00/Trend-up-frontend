Feature('products - 상품을 사려는 사람은 원하는 상품을 찾기 위해 카테고리, 검색을 통해 상품 목록을 볼 수 있다.');

Scenario('카테고리 선택 - 해당 카테고리에 아이템이 존재하지 않을 경우', ({ I }) => {
  // Given

  // When

  // Then

});

Scenario('카테고리 선택 - 해당 카테고리에 아이템이 10개인 경우', ({ I }) => {
  // Given
  I.setupProducts();

  // When

  // Then
});

Scenario('검색 - 해당 검색어에 대한 아이템이 존재하지 않을 경우', ({ I }) => {
  // Given

  // When

  // Then

});

Scenario('검색 - 해당 검색어에 대한 아이템이 10개인 경우', ({ I }) => {
  // Given

  // When

  // Then

});

Scenario('정렬을 바꾸는 경우', ({ I }) => {
  // Given

  // When

  // Then

});
