Feature('장바구니');

Scenario('장바구니에서 전체 선택 후 주문(총 2개)', ({ I }) => {
  // Given

  // When

  // Then

});

Scenario('장바구니 상품 1개 선택 후 주문(총 2개)', ({ I }) => {
  // Given

  // When

  // Then

});

Scenario('아무것도 선택하지 않고 주문하는 경우', ({ I }) => {
  // Given

  // When

  // Then

});
